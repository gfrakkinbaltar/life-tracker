import type { Metadata } from 'next'
import { Inter } from 'next/font/google'
import './globals.css'

const inter = Inter({ 
  subsets: ['latin'],
  variable: '--font-inter',
  display: 'swap',
})

export const metadata: Metadata = {
  title: 'Life Tracker - Personal Analytics Platform',
  description: 'Turn your life into a beautiful, comprehensive data story. Discover patterns, optimize habits, visualize your journey.',
  keywords: ['quantified self', 'life tracking', 'personal analytics', 'health tracking', 'productivity'],
}

export default function RootLayout({
  children,
}: {
  children: React.ReactNode
}) {
  return (
    <html lang="en" className="dark">
      <body className={`${inter.variable} antialiased`}>
        {children}
      </body>
    </html>
  )
}

